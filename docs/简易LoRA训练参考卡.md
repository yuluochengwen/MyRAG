# 🎯 简易 LoRA 训练 - 快速参考卡

## 一键启动

```powershell
.\start-training.bat
```

## 访问地址

```
训练页面: http://localhost:8000/static/simple-lora-training.html
模型管理: http://localhost:8000/static/model-management.html
```

## 使用步骤

1. 上传数据集（拖拽或点击）
2. 选择基座模型
3. 输入任务名称
4. 点击"开始训练"

## 数据集格式

### Alpaca 格式

```json
[
    {
        "instruction": "问题",
        "input": "",
        "output": "答案"
    }
]
```

### ShareGPT 格式

```json
[
    {
        "conversations": [
            {"from": "human", "value": "用户消息"},
            {"from": "gpt", "value": "AI回复"}
        ]
    }
]
```

## 自动参数

- LoRA Rank: **16**
- 训练轮次: **3 epochs**
- 批次大小: **4** (有效 batch=16)
- 学习率: **2e-4**
- 量化: **4-bit QLoRA**

## 输出位置

```
Models/LoRA/<任务名>_<时间戳>/
├── adapter_config.json
├── adapter_model.safetensors
└── tokenizer 配置文件
```

## 训练时间估算

| 样本数 | 时间 (RTX 3060) |
|--------|----------------|
| 10 | ~5 分钟 |
| 100 | ~15 分钟 |
| 500 | ~40 分钟 |
| 2000 | ~2.5 小时 |

## API 快速参考

```http
# 获取模型列表
GET /api/simple-lora/models

# 上传数据集
POST /api/simple-lora/upload-dataset

# 创建训练任务
POST /api/simple-lora/train

# 查询任务状态
GET /api/simple-lora/tasks/{id}

# 获取所有任务
GET /api/simple-lora/tasks
```

## 常见问题

### 显存不足？
- 关闭其他程序
- 重启电脑

### 数据格式错误？
- 检查 JSON 语法
- 使用示例数据集测试

### 训练失败？
- 查看任务状态消息
- 检查后端日志

## 示例数据集

系统已提供：
```
TrainingData/example_alpaca_dataset.json
```

直接使用，包含 10 个样本！

## 完整文档

- 📖 `docs/简易LoRA训练快速开始.md`
- 📚 `docs/简易LoRA训练功能说明.md`
- 📝 `docs/简易LoRA训练实现总结.md`
